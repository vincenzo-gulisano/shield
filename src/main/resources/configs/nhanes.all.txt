$basePath = ''outputs/{^^.name}/{^^.startTime}/''
$minTreeH = 4
$maxTreeH = 16
$imgType = svg
$nPop = 100

ea.experiment(
  runs = (randomGenerator = (seed = [1:1:5])
    * [m.defaultRG()])
    * (solver = (stop = ea.sc.nOfQualityEvaluations(n = 10000))
    * [
      %ea.s.mapElites(
      %  nPop = 100; % note: maybe pick according to possible number of cells of the archive, because map elites keeps only 1 and only 1 per cell
      %  representation = ea.r.cfgTree(
      %    minTreeH = $minTreeH;
      %    maxTreeH = $maxTreeH;
      %    grammar = ea.grammar.fromFile(
      %      path = "src/main/resources/grammars/nhanes/nhanes.all.bnf"));
      %  mapper = anonym.mapper.queryMapper(
      %    fieldValueSampler = anonym.valueSampler.nhanesTupleFieldValueSampler(
      %      inputCsvPath = "datasets/nhanes.csv";
      %      seed = 0
      %    )
      %  );
      %  descriptors = [
      %    ea.s.me.d.descriptor(f = anonym.f.graphFiltersCount(of = ea.f.solution()); min = 0; max = 4; nOfBins = 5);
      %    ea.s.me.d.descriptor(f = anonym.f.graphForksCount(of = ea.f.solution()); min = 0; max = 4; nOfBins = 5)
      %  ]
      %)%;
      ea.s.nsga2(
        nPop = 100;
        representation = ea.r.cfgTree(
          minTreeH = $minTreeH;
          maxTreeH = $maxTreeH;
          grammar = ea.grammar.fromFile(
            path = "src/main/resources/grammars/nhanes/nhanes.all.bnf"));
        mapper = anonym.mapper.queryMapperWithSampler(
          fieldValueSampler = anonym.valueSampler.nhanesTupleFieldValueSampler(
            inputCsvPath = "datasets/nhanes.csv";
            seed = 0
          )
        )
      )
    ])
    * [
    ea.run(
      problem = anonym.problem.nhanesStreamAnonymizationProblem(
        inputCsvPath = "datasets/nhanes.csv";
        privacyMetric = K_ANONYMITY_CARDINALITY_Q99
      )
    )
  ];
  listeners = [
    anonym.l.liebreTerminator();
    ea.l.console(eFunctions = [
      ea.f.size(of = ea.f.genotype(of = ea.f.best()); format = "%3d")
    ]);
    ea.l.csv(
      path = $basePath + "solutions-percentile.csv";
      eFunctions = [
        f.mapValue(key = privacy; of = ea.f.quality(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality()))));
        f.mapValue(key = "semantics"; of = ea.f.quality(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality()))));
        f.mapValue(key = "fidelity"; of = ea.f.quality(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality()))));
        ea.f.solution(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality())));

        f.mapValue(key = privacy; of = ea.f.quality(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = "semantics"; of = ea.f.quality()))));
        f.mapValue(key = "semantics"; of = ea.f.quality(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = "semantics"; of = ea.f.quality()))));
        f.mapValue(key = "fidelity"; of = ea.f.quality(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = "semantics"; of = ea.f.quality()))));
        ea.f.solution(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = "semantics"; of = ea.f.quality())));

        f.mapValue(key = privacy; of = ea.f.quality(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = "fidelity"; of = ea.f.quality()))));
        f.mapValue(key = "semantics"; of = ea.f.quality(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = "fidelity"; of = ea.f.quality()))));
        f.mapValue(key = "fidelity"; of = ea.f.quality(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = "fidelity"; of = ea.f.quality()))));
        ea.f.solution(of = f.percentile(p = 100; of = ea.f.all(); by = f.mapValue(key = "fidelity"; of = ea.f.quality())));

        f.mapValue(key = privacy; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality()))));
        f.mapValue(key = "semantics"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality()))));
        f.mapValue(key = "fidelity"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality()))));
        ea.f.solution(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality())));

        f.mapValue(key = privacy; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "semantics"; of = ea.f.quality()))));
        f.mapValue(key = "semantics"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "semantics"; of = ea.f.quality()))));
        f.mapValue(key = "fidelity"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "semantics"; of = ea.f.quality()))));
        ea.f.solution(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "semantics"; of = ea.f.quality())));

        f.mapValue(key = privacy; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "fidelity"; of = ea.f.quality()))));
        f.mapValue(key = "semantics"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "fidelity"; of = ea.f.quality()))));
        f.mapValue(key = "fidelity"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "fidelity"; of = ea.f.quality()))));
        ea.f.solution(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "fidelity"; of = ea.f.quality())))
      ]
    );
    ea.l.savePlotForRun(
      path = $basePath + "{solver.name}/{problem.name}/{randomGenerator.seed}/" + "scatter-fidelity-vs-semantics.svg";
      plot = ea.plot.single.biObjectivePopulation(
        x = f.mapValue(key = "fidelity"; of = ea.f.quality());
        y = f.mapValue(key = "semantics"; of = ea.f.quality())
      );
      configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
      secondary = true;
      type = svg
    );
    ea.l.savePlotForRun(
      path = $basePath + "{solver.name}/{problem.name}/{randomGenerator.seed}/" + "scatter-privacy-vs-semantics.svg";
      plot = ea.plot.single.biObjectivePopulation(
        x = f.mapValue(key = privacy; of = ea.f.quality());
        y = f.mapValue(key = "semantics"; of = ea.f.quality())
      );
      configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
      secondary = true;
      type = svg
    );
    ea.l.savePlotForRun(
      path = $basePath + "{solver.name}/{problem.name}/{randomGenerator.seed}/" + "scatter-fidelity-vs-privacy.svg";
      plot = ea.plot.single.biObjectivePopulation(
        x = f.mapValue(key = "fidelity"; of = ea.f.quality());
        y = f.mapValue(key = privacy; of = ea.f.quality())
      );
      configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
      secondary = true;
      type = svg
    );
    ea.l.savePlotForExp(
      path = $basePath + "p75privacy-semantics.svg";
      plot = ea.plot.multi.xyExp(
        y = f.mapValue(key = "semantics"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality()))))
      );
      configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
      type = svg
    );
    ea.l.savePlotForExp(
      path = $basePath + "p75privacy-fidelity.svg";
      plot = ea.plot.multi.xyExp(
        y = f.mapValue(key = "fidelity"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = privacy; of = ea.f.quality()))))
      );
      configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
      type = svg
    );
    ea.l.savePlotForExp(
      path = $basePath + "p75semantics-privacy.svg";
      plot = ea.plot.multi.xyExp(
        y = f.mapValue(key = "privacy"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "semantics"; of = ea.f.quality()))))
      );
      configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
      type = svg
    );
    ea.l.savePlotForExp(
      path = $basePath + "p75semantics-fidelity.svg";
      plot = ea.plot.multi.xyExp(
        y = f.mapValue(key = "fidelity"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "semantics"; of = ea.f.quality()))))
      );
      configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
      type = svg
    );
    ea.l.savePlotForExp(
      path = $basePath + "p75fidelity-privacy.svg";
      plot = ea.plot.multi.xyExp(
        y = f.mapValue(key = privacy; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "fidelity"; of = ea.f.quality()))))
      );
      configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
      type = svg
    );
    ea.l.savePlotForExp(
      path = $basePath + "p75fidelity-semantics.svg";
      plot = ea.plot.multi.xyExp(
        y = f.mapValue(key = "semantics"; of = ea.f.quality(of = f.percentile(p = 75; of = ea.f.all(); by = f.mapValue(key = "fidelity"; of = ea.f.quality()))))
      );
      configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
      type = svg
    );
    ea.l.savePlotForExp(
          path = $basePath + "p75-privacy.svg";
          plot = ea.plot.multi.xyExp(y = f.percentile(p = 75; of = f.each(of = ea.f.all(); mapF = f.mapValue(key = privacy; of = ea.f.quality()))));
          configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
          type = svg
    );
    ea.l.savePlotForExp(
          path = $basePath + "p75-semantics.svg";
          plot = ea.plot.multi.xyExp(y = f.percentile(p = 75; of = f.each(of = ea.f.all(); mapF = f.mapValue(key = "semantics"; of = ea.f.quality()))));
          configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
          type = svg
    );
    ea.l.savePlotForExp(
          path = $basePath + "p75-fidelity.svg";
          plot = ea.plot.multi.xyExp(y = f.percentile(p = 75; of = f.each(of = ea.f.all(); mapF = f.mapValue(key = "fidelity"; of = ea.f.quality()))));
          configuration = viz.plot.conf.image(axesShow = all; axesIndependences = [all]);
          type = svg
    );
    ea.l.savePlotForRun(
      path = $basePath + "{solver.name}/{problem.name}/{randomGenerator.seed}/archive." + $imgType;
      plot = ea.plot.single.me(
        values = [
          f.mapValue(key = privacy; of = ea.f.quality());
          f.mapValue(key = fidelity; of = ea.f.quality());
          f.mapValue(key = semantics; of = ea.f.quality())
        ]
      );
      kCondition = predicate.matches(f = f.mappableKey(key = "solver.name"); regex = "me"); % this one is to ensure we create these graphs only when the sover is map elites
      type = $imgType
    )
  ]
)
